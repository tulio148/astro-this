# Do not strip trailing slashes. Astro's default static output uses directory
# routes, and the production host redirects /path to /path/. Adding the
# opposite redirect here creates a /path <-> /path/ loop.
