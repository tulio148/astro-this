---
import SectionContainer from "./SectionContainer.astro";
import SectionTitle from "./SectionTitle.astro";
import FAQItem from "./FAQItem.astro";

interface FAQ {
  question: string;
  answer: string;
}

interface Props {
  title: string;
  subtitle?: string;
  faqs: FAQ[];
  id?: string;
  tone?: "light" | "dark-band" | "tint";
}

const { title, subtitle, faqs = [], id, tone = "light" } = Astro.props;
---

<SectionContainer id={id} tone={tone}>
  <SectionTitle title={title} subtitle={subtitle} class="section-intro" />
  <div class="mx-auto max-w-5xl">
    {faqs.map((faq) => <FAQItem question={faq.question} answer={faq.answer} />)}
  </div>
</SectionContainer>
